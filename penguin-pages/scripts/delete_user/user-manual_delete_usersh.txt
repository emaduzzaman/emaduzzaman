#reusable cleanup script (delete_user.sh) that will:
- Take a username as input.
- Check if the user exists or not.
- Delete all leftover files, crontabs, groups, sudo entries, etc.
- Print out what it’s doing (so you know exactly what was deleted).

How to use delete_user.sh
1. Save the script as `delete_user.sh`.
2. Make it executable: chmod +x delete_user.sh
3. Run it with the username: ./delete_user.sh username
