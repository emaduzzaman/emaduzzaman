sudo apt update && sudo apt upgrade -y (lags and slow)
#vm was too slow to use ( the issue was when i try to sudo apt update && sudo apt upgrade it lages and shtis)
#solution
#step1
sudo nano /etc/cloud/cloud.cfg.d/99-disable-network-config.cfg
Add: network: {config: disabled}
Save and exit.
#STEP 2 — Create your own permanent Netplan file
sudo nano /etc/netplan/01-netcfg.yaml
network: version: 2 ethernets: ens3: dhcp4: true nameservers: addresses: - 8.8.8.8 - 1.1.1.1
sudo netplan generate sudo netplan apply

-----------------------------------------
sudo hostnamectl set-hostname emad-dns
hostnamectl
#Output should show
Static hostname: emad-dns
apt update && upgrade -y
sudo apt install -y bind9 bind9utils bind9-doc dnsutils   
#Pending kernel upgrade,Newer kernel available,The currently running kernel version is 5.15.0-143-generic which is not the expected kernel version 5.15.0-163-generic,Restarting the system to #load the new kernel will not be handled automatically, so you should consider rebooting,<0k>,
#Check kernel versions:
uname -r
dpkg --list | grep linux-image
#Reboot to load the new kernel: sudo reboot
#After reboot:
uname -r
#You can remove the old kernel to save space:
sudo apt remove linux-image-5.15.0-143-generic
sudo update-grub
----------------------------------------------
sudo systemctl status named   
#cchekc if this is running or not 
sudo ufw allow 53   
#allow prot


